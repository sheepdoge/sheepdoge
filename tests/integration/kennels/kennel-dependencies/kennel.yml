---

- name: kennel-dependencies
  hosts: localhost

  vars_files:
    - vars/var.yml

  tasks:
    - include_role:
        name: sheepdog.pup-base

    - include_role:
        name: sheepdog.pup-dependencies
