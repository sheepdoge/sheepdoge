---
- name: Ensure cron is installed.
  apt:
    name: "{{ item }}"
    update_cache: yes
  with_items:
    - cron

# For some reason, the `service` module does not work for this.
- name: Start cron service.
  command: |
    service cron start
