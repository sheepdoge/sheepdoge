---
- name: Create the secret file if it does not already exist.
  file:
    state: touch
    path: "{{ secret_file }}"
    mode: u+rw

- name: Append the secret file stored as a variable.
  lineinfile:
    path: "{{ secret_file }}"
    line: "{{ encrypted_nightly }}-{{ 100 | random }}"
